{
  "name": "spexx-cloud-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies branded API domains to Supabase projects for Spexx apps.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.80.0"
  }
}
